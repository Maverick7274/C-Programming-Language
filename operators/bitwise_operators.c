#include <stdio.h>
int main() {
    int a = 12, b = 25;
    // AND Operator (Bitwise AND)
    printf("Output = %d", a&b);

    // OR Operator (Bitwise OR)
    printf("Output = %d", a|b);

    // XOR Operator (Bitwise XOR)
    printf("Output = %d", a^b);
}

